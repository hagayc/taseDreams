# Infrastructure Simulation Environment
# Contains: Bitbucket, Confluence, LDAP Server
# Note: OCP, ArgoCD, and Tekton typically run on Kubernetes/OCP itself
# Run with: docker-compose -f docker-compose.infrastructure.yaml up -d
# Memory Requirements: ~6-8 GB RAM (without OCP, without Artifactory)

services:
  # Bitbucket Server
  bitbucket:
    build:
      context: ./infra/bitbucket
      dockerfile: Dockerfile
    container_name: infrastructure-bitbucket
    environment:
      - SETUP_LICENSE=yes
      - JVM_MINIMUM_MEMORY=768m
      - JVM_MAXIMUM_MEMORY=1536m
      # Plugin download URLs (optional - if plugins are not in plugins/ directory)
      - SCRIPTRUNNER_PLUGIN_URL=${SCRIPTRUNNER_PLUGIN_URL:-}
      - CONTROLFREAK_PLUGIN_URL=${CONTROLFREAK_PLUGIN_URL:-}
    ports:
      - "${BITBUCKET_PORT:-7990}:7990"
      - "${BITBUCKET_SSH_PORT:-7999}:7999"
    volumes:
      - bitbucket_data:/var/atlassian/application-data/bitbucket
    networks:
      - infrastructure-network
    mem_limit: 3g
    mem_reservation: 1.5g
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7990/status"]
      interval: 30s
      timeout: 10s
      retries: 5

  # PostgreSQL Database for Confluence
  confluence-db:
    image: postgres:15-alpine
    container_name: infrastructure-confluence-db
    environment:
      - POSTGRES_DB=${CONFLUENCE_DB_NAME:-confluence}
      - POSTGRES_USER=${CONFLUENCE_DB_USER:-confluence}
      - POSTGRES_PASSWORD=${CONFLUENCE_DB_PASSWORD:-confluence}
    volumes:
      - confluence_db_data:/var/lib/postgresql/data
    networks:
      - infrastructure-network
    mem_limit: 768m
    mem_reservation: 384m
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${CONFLUENCE_DB_USER:-confluence}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Confluence
  confluence:
    image: atlassian/confluence-server:latest
    container_name: infrastructure-confluence
    environment:
      - ATL_TOMCAT_MGMTPORT=8000
      - JVM_MINIMUM_MEMORY=768m
      - JVM_MAXIMUM_MEMORY=1536m
      # Database configuration
      - ATL_JDBC_URL=jdbc:postgresql://confluence-db:5432/${CONFLUENCE_DB_NAME:-confluence}
      - ATL_JDBC_USER=${CONFLUENCE_DB_USER:-confluence}
      - ATL_JDBC_PASSWORD=${CONFLUENCE_DB_PASSWORD:-confluence}
      - ATL_DB_TYPE=postgresql
    ports:
      - "${CONFLUENCE_PORT:-8090}:8090"
    volumes:
      - confluence_data:/var/atlassian/application-data/confluence
    networks:
      - infrastructure-network
    depends_on:
      confluence-db:
        condition: service_healthy
    mem_limit: 3g
    mem_reservation: 1.5g
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8090/status"]
      interval: 30s
      timeout: 10s
      retries: 5

  # JFrog Artifactory (commented out - high memory usage)
  # artifactory:
  #   image: releases-docker.jfrog.io/jfrog/artifactory-pro:latest
  #   container_name: infrastructure-artifactory
  #   restart: unless-stopped
  #   environment:
  #     - EXTRA_JAVA_OPTIONS=-Xms1g -Xmx3g
  #     - JF_ARTIFACTORY_OPERATOR_ENABLED=false
  #     - JF_ACCESS_OPERATOR_ENABLED=false
  #     - ARTIFACTORY_ADMIN_USERNAME=${ARTIFACTORY_ADMIN_USERNAME:-admin}
  #     - ARTIFACTORY_ADMIN_PASSWORD=${ARTIFACTORY_ADMIN_PASSWORD:-admin}
  #   ports:
  #     - "${ARTIFACTORY_PORT:-8081}:8081"
  #     - "${ARTIFACTORY_PORT_8082:-8082}:8082"
  #   volumes:
  #     - artifactory_data:/var/opt/jfrog/artifactory
  #   networks:
  #     - infrastructure-network
  #   mem_limit: 6g
  #   mem_reservation: 3g
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:8081/artifactory/api/system/ping"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 10
  #     start_period: 120s

  # LDAP Server (OpenLDAP)
  ldap:
    image: osixia/openldap:latest
    container_name: infrastructure-ldap
    environment:
      - LDAP_ORGANISATION=${LDAP_ORGANISATION:-TaseDreams}
      - LDAP_DOMAIN=${LDAP_DOMAIN:-tasedreams.local}
      - LDAP_ADMIN_PASSWORD=${LDAP_ADMIN_PASSWORD:-admin}
      - LDAP_CONFIG_PASSWORD=${LDAP_CONFIG_PASSWORD:-config}
      - LDAP_READONLY_USER=${LDAP_READONLY_USER:-false}
    ports:
      - "${LDAP_PORT:-389}:389"
      - "${LDAP_SSL_PORT:-636}:636"
    volumes:
      - ldap_data:/var/lib/ldap
      - ldap_config:/etc/ldap/slapd.d
    networks:
      - infrastructure-network
    mem_limit: 768m
    mem_reservation: 384m
    healthcheck:
      test: ["CMD", "ldapsearch", "-x", "-H", "ldap://localhost", "-b", "dc=tasedreams,dc=local"]
      interval: 30s
      timeout: 10s
      retries: 5

  # LDAP Admin UI (phpLDAPadmin)
  ldap-admin:
    image: osixia/phpldapadmin:latest
    container_name: infrastructure-ldap-admin
    environment:
      - PHPLDAPADMIN_LDAP_HOSTS=ldap
      - PHPLDAPADMIN_HTTPS=false
    ports:
      - "${LDAP_ADMIN_PORT:-8080}:80"
    depends_on:
      - ldap
    networks:
      - infrastructure-network
    mem_limit: 384m
    mem_reservation: 192m

volumes:
  bitbucket_data:
  confluence_data:
  confluence_db_data:
  # artifactory_data:  # Commented out with Artifactory service
  ldap_data:
  ldap_config:

networks:
  infrastructure-network:
    driver: bridge

