apiVersion: v1
kind: Service
metadata:
  name: {{ include "tasedreams-frontend.fullname" . }}
  labels:
    {{- include "tasedreams-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tasedreams-frontend.selectorLabels" . | nindent 4 }}

