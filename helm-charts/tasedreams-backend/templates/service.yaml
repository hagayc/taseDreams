apiVersion: v1
kind: Service
metadata:
  name: {{ include "tasedreams-backend.fullname" . }}
  labels:
    {{- include "tasedreams-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tasedreams-backend.selectorLabels" . | nindent 4 }}

